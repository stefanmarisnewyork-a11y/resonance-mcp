services:
  - type: web
    name: resonanceos-mcp
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
      - key: RESONANCE_TRANSPORT
        value: "http"
